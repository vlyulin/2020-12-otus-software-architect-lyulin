apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hw08-idempotency-auth.fullname" . }}-test-connection"
  labels:
    {{- include "hw08-idempotency-auth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hw08-idempotency-auth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
